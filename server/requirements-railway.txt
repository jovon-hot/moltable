# Moltable API — production requirements (Railway)
# 精简生产依赖：只装运行时必需的包，不装本地开发/ML 栈（torch 等）。
# 版本与 server/.venv 实测验证版本一致。
# embedding 无 sentence-transformers 时自动降级 trigram hash，功能可工作。
altcha==2.1.0
fastapi==0.141.1
uvicorn[standard]==0.52.1
supabase==2.31.0
openai==2.53.0
python-dotenv==1.2.2
pydantic==2.13.4
slowapi==0.1.10
stripe==15.4.0
cryptography==50.0.0
pyjwt==2.13.0
httpx==0.28.1
