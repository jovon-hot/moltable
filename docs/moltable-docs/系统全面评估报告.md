# Moltable 系统全面评估报告

> 日期：2026-07-07 | 评估模式：实机测试 + Agent 视角

---

## 一、系统状态总览

### 运行环境
```
后端:  FastAPI v0.1.0 | port 8642 | in-memory 模式 (无 Supabase)
前端:  Next.js 14     | port 8701 | demo 模式 (无 Supabase)
协议:  MCP JSON-RPC 2.0 | .well-known/mcp 发现端点
```

### 服务健康检查

| 端点 | 状态 | 说明 |
|------|:---:|------|
| `/health` | ✅ | `{"status":"ok"}` |
| `.well-known/mcp` | ✅ | 完整 MCP 发现信息 |
| `/docs` (Swagger) | ✅ | 20 个 API 端点可交互 |
| `/mcp` (JSON-RPC) | ✅ | 认证后可用 |
| Frontend `/` | ✅ | OnePage 完整渲染 |
| Frontend `/docs` | ✅ | 文档中心完整 |
| Frontend `/dashboard` | ✅ | 演示模式正常 |

### MCP 工具清单 (12 个)

| 工具 | 类型 | 认证 | 状态 |
|------|------|:---:|:---:|
| `ping` | 心跳 | ❌ **Bug** | 应免认证 |
| `auto_provision` | 核心 | ✅ | 一键配置 |
| `search_memory` | 核心 | ✅ | 语义搜索 |
| `save_memory` | 核心 | ✅ | 单条保存 |
| `save_memories` | 新增 | ✅ | 批量保存 |
| `search_by_tag` | 新增 | ✅ | 标签搜索 |
| `list_personas` | 核心 | ✅ | Persona 列表 |
| `get_persona` | 核心 | ✅ | Persona 详情 |
| `match_persona` | 新增 | ✅ | 智能推荐 |
| `compare_personas` | 新增 | ✅ | 多视角对比 |
| `consult_persona` | 核心 | ✅ | Persona 问答 |
| `archive_memory` | 新增 | ✅ | 软删除 |

---

## 二、MCP 协议合规性评估

### 通过项
- ✅ `.well-known/mcp` 发现端点正确实现
- ✅ JSON-RPC 2.0 协议格式正确
- ✅ `initialize` 握手返回 serverInfo
- ✅ 批量请求支持
- ✅ 标准错误码 (-32001 Auth, -32601 MethodNotFound, -32600 InvalidRequest)

### 发现问题

**🔴 BUG: `ping` 工具要求认证**
```json
// ping 不需要用户上下文，MCP 规范中应免认证
// 当前返回: {"error": {"code": -32001, "message": "Authentication required"}}
```
修复: `mcp.py` 中 `_IP_AWARE_TOOLS` 集合不应包含 `ping`

**🟡 In-memory 模式无法注册用户**
- 无 Supabase 时 `POST /api/auth/api-keys` 无法创建 API Key
- Agent 无法在 in-memory 模式下完成注册→获取 Key→调用工具的完整流程
- 建议: in-memory 模式预置一个 demo API Key 供测试

**🟡 `tools/list` 同样需要认证**
- MCP 规范允许 `tools/list` 在认证前返回工具列表
- 当前实现统一要求 X-API-Key header

---

## 三、Agent 注册流程分析

### 当前流程 (需要 Supabase)

```
Agent 连接 Moltable
    ↓
发现用户未注册 → 提示用户访问 moltable.ai/register
    ↓
用户在前端注册 → 获取 API Key (SHA-256 哈希存储)
    ↓
用户将 API Key 提供给 Agent
    ↓
Agent 调用 auto_provision() → 加载用户画像
    ↓
Agent 开始工作
```

### 问题：Agent 无法自主注册

当前架构中，注册必须由用户在前端完成。Agent 不能：
- 替用户创建账号
- 生成 API Key
- 完成首次配置

### 建议：Agent 辅助注册流程

```
Agent 连接 Moltable → 无 API Key
    ↓
Agent: "我注意到你还没有 Moltable 账号。要现在创建一个吗？
        30 秒搞定，之后我就能记住我们所有的对话。"
    ↓
用户同意 → Agent 打开注册页 OR 通过 API 代理注册
    ↓
Agent 获取 API Key → 自动配置 → 开始工作
```

这个引导流程对应到代码层面，需要在以下时机触发：
1. Agent 启动时检测是否有 Moltable 配置
2. 如果没有 → 在合适的对话间隙主动引导
3. 引导要自然，不能打断用户当前任务

---

## 四、前端可用性评估

### 页面完整度

| 页面 | 内容 | i18n | 状态 |
|------|------|:---:|:---:|
| `/` | OnePage (Hero→Features→How→Pricing→About→Privacy) | zh/en | ✅ |
| `/docs` | 完整文档中心 (12 API + MCP 协议 + FAQ) | zh/en | ✅ |
| `/dashboard` | 仪表盘演示 | zh/en | ✅ |
| `/dashboard/memories` | 记忆管理演示 | zh/en | ✅ |
| `/dashboard/personas` | Persona 演示 | zh/en | ✅ |
| `/dashboard/settings` | 设置演示 | zh/en | ✅ |
| `/login` | 登录页 | - | ✅ |
| `/register` | 注册页 | - | ✅ |
| `/blog` | 占位 | - | ⚠️ |

### 设计质量
- 暗色 Linear 风格，专业感强
- 紫色 (#7170ff) accent 使用克制
- Inter 字体 + shadow-as-border 体系
- 全部 Tailwind CSS class（零内联 style）

### 交互体验
- ✅ 导航锚点跨页面平滑跳转
- ✅ Dashboard 演示模式无需登录
- ✅ 中英文切换持久化
- ⚠️ 注册/登录依赖 Supabase（本地不可用）
- ⚠️ 博客页为占位

---

## 五、总体评分

| 维度 | 评分 | 说明 |
|------|:----:|------|
| 后端架构 | **A** (9/10) | 清晰分层，Repository 模式，in-memory 降级 |
| MCP 协议 | **B+** (8/10) | 格式正确，但 ping 不应需认证 |
| API 完整性 | **A** (9/10) | 20 REST + 12 MCP 工具 |
| 前端设计 | **A** (9/10) | Linear 精密暗色，零内联 style |
| 前端功能 | **B+** (8/10) | OnePage+i18n+Demo，但注册链路不通 |
| 文档 | **A** (9/10) | 12 API 全文档含 curl 示例 |
| Agent 体验 | **B** (7/10) | auto_provision 优秀，但无法自主注册 |
| 开箱即用 | **B** (7/10) | dev 模式可演示，生产需 Supabase |
| **综合** | **B+ (82/100)** | 优秀产品，注册链和免认证是最后短板 |

---

## 六、关键改进建议

### 🔴 P0 — 阻塞 Agent 使用

| # | 问题 | 修复 |
|---|------|------|
| 1 | `ping` 不应需认证 | mcp.py: 将 ping 从认证检查中排除 |
| 2 | In-memory 模式无法创建用户 | 预置 demo API Key + 内存用户表 |

### 🟡 P1 — 提升体验

| # | 问题 | 修复 |
|---|------|------|
| 3 | Agent 无法引导注册 | 在 Hermes Skill 中添加注册引导流程 |
| 4 | `tools/list` 应免认证 | 允许未认证客户端发现工具 |
| 5 | 博客页为占位 | 补充 1-2 篇文章或改为 changelog |

---

## 七、Agent 引导注册流程设计

当 Agent（通过 Moltable Skill）连接时且无 API Key：

```
[Moltable Skill 自动检测]
   ↓
[没有 API Key 配置]
   ↓
Agent 在首次对话合适时机主动引导：

"我发现你还没有配置 Moltable。Moltable 能让我记住我们之间的所有对话——
你的偏好、项目、决策都会跨 AI 平台同步。

要不要花 30 秒注册一个？之后:
- 你在 Claude 里的偏好，在 Hermes 里我也知道
- 我会自动积累工作习惯，越用越智能
- 你可以创建多个 Persona（战略顾问/审核员/写手）

👉 注册地址: https://moltable.ai/register
注册后把 API Key 告诉我，或者设置环境变量 MOLTABLE_API_KEY。
"
```

这个引导应该在以下时机触发：
1. 用户首次与 Agent 对话时
2. 用户提到需要"记住"、"长期项目"、"跨平台"时
3. 不要在用户处理紧急任务时打断
