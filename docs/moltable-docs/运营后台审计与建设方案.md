# Moltable 运营后台与数据统计审计报告

> 日期：2026-08-01 | 审计范围：运营后台 / 数据统计 / 日志 / 告警 / 用户生命周期 / 账单 / 备份

---

## 一、运营就绪度评分

| 维度 | 评分 | 状态 | 关键缺口 |
|------|:---:|:---:|------|
| **运营后台** | 0/100 | 🔴 不存在 | 无 Admin Dashboard、无管理员 API、无用户管理 |
| **数据统计** | 5/100 | 🔴 几乎空白 | 无 DAU/MAU/注册量/激活率/留存率任一种 |
| **日志系统** | 30/100 | 🔴 裸 stdout | 有请求日志中间件和 error_count，但无集中存储/检索/保留策略 |
| **告警机制** | 5/100 | 🔴 无 | 无任何告警通道（API 500 静默崩溃） |
| **用户生命周期** | 15/100 | 🔴 无追踪 | 有 users 表和 audit_logs，但无激活/活跃/流失标记 |
| **账单和配额** | 40/100 | 🟡 基本框架有 | 有计划体系 + quota 检查，但无 Admin 管理和到期处理 |
| **数据备份** | 20/100 | 🟡 依赖 Supabase | 有提及但未验证，无异地/手动备份方案 |

### **综合运营就绪度: 16/100 — ❌ 严重不足**

---

## 二、逐项详细审计

### 2.1 运营后台 — 缺失清单

**现状：零。** 前端无 `/admin` 页面，后端无任何 admin 路由。

**代码证据：**
- `web/src/app/` 下无 `admin/` 目录
- `server/routes/` 下无 `admin.py`
- DB schema 无 `admin` 角色或 `is_admin` 字段
- 前端 `settings` 页面仅为用户自管理（API Key + 用量），无管理视角

**缺了什么：**
- 用户列表 / 搜索 / 封禁
- 全局数据看板（总用户数、DAU、注册趋势）
- 用量排行 / 异常用户检测
- 系统配置热更新（trial 开关、限流参数）

---

### 2.2 数据统计 — 缺失清单

**现状：零统计基础设施。** 全部需要新增。

**代码证据：**
- `schema.sql` 无统计相关表（无 `daily_stats`、`events`、`user_actions`）
- `quota.py` 仅做实时 count 不做快照
- `/health` 仅返回 error_count，无业务指标
- 前端 Dashboard 仅显示个人 stats（memories/personas 计数）

**缺失指标：**
- DAU / WAU / MAU
- 新注册量（每日）
- 试用激活率（注册→激活转化）
- 7/30 日留存率
- API 调用量（按用户/按端点）
- 各功能使用渗透率（Persona 创建率 / Memory 创建率）

---

### 2.3 日志系统 — 现状

**有：**
- `request_logging_middleware`：记录 method + path + status + duration 到 stdout（`main.py:98-117`）
- `record_error()` / `get_error_count()`：内存 deque，1小时窗口，线程安全（`app_state.py:83-101`）
- `audit_logs` 表：记录 auth 失败（IP + reason）
- Python `logging.basicConfig`：INFO 级别输出到 stdout
- Railway `/health` 仅返回 error_count

**缺失：**
- 日志无集中存储（纯 stdout，Railway 重启即丢失）
- 无结构化日志格式（JSON）
- 无日志检索（不能搜某用户的操作历史 / 某时间段的异常）
- 无日志级别控制（不能动态调整 DEBUG/ERROR）
- Go backend（`cmd/server`）使用 Zap JSON 日志，Python backend 未对齐

---

### 2.4 告警机制

**现状：完全缺失。**

- `global_exception_handler` 调用 `record_error()` 后仅返回 500
- 没有任何通知通道（邮件/企微/短信/Webhook）
- 没有触发规则（连续N次500、DB连接断开、高延迟）

**Railway 能做什么：**
- Railway 提供 CPU/Memory 监控面板和日志流
- 但无自定义告警规则（如 API 500 ≥ 5/min）

---

### 2.5 用户生命周期管理

**现状：只有 users 表，无生命周期状态机。**

**users 表字段：**
```
id, email, name, password_hash, plan, timezone, language, created_at, updated_at
```

**缺失：**
- `last_active_at`（最后活跃时间）
- `status`（active / inactive / churned / banned）
- `activated_at`（试用激活时间）
- `trial_end_at`（试用到期时间 — 在 billing.py 中计算但未持久化）
- `source`（注册来源：web / cli / referral）
- `email_verified`（邮箱验证状态）

---

### 2.6 账单和配额管理

**有：**
- 三层计划体系：free / pro / team（`quota.py`）
- 90 天试用激活端点（`billing.py: POST /api/billing/activate`）
- `get_usage()` 实时查询用量
- Stripe 配置变量已预留（`STRIPE_SECRET_KEY` 等）
- Plan 对比展示（前端 pricing 页）

**缺口：**
- 试用到期后无自动降级逻辑（plan 永远不自动回到 free）
- 无账单历史记录表
- 无用量超限通知
- 无发票 / 收据
- 无管理员手动调整配额能力
- Stripe webhook 未实现

---

### 2.7 数据备份策略

**现状：**
- Supabase：自动每日备份（需 Pro plan 确认）
- Railway：无数据库（仅为 API 运行时），不涉及备份
- `docker-compose.yml` 中 PostgreSQL volume: `pgdata`（本地开发）
- `DEPLOYMENT_GUIDE.md` 提到「Supabase 有自动备份，确认 Pro plan」（未验证）

**风险：**
- Supabase Free plan 不提供自动备份
- 无异地备份
- 无手动备份脚本
- 无恢复演练

---

## 三、必须搭建的后台功能清单（按优先级）

### 🔴 P0 — 立即建设（1-2 周，阻塞运营）

| # | 功能 | 说明 | 后端工作量 | 前端工作量 |
|---|------|------|:---:|:---:|
| 1 | **Admin 认证** | `is_admin` 字段 + admin middleware | 0.5d | — |
| 2 | **用户管理 API** | `GET /api/admin/users` 列表+搜索+分页 | 1d | — |
| 3 | **用户管理页面** | Admin Dashboard 首页 + 用户列表 | — | 1d |
| 4 | **数据统计表** | `daily_stats` 表 + 定时聚合脚本 | 1.5d | — |
| 5 | **基础统计看板** | 总用户数、DAU、注册趋势图 | — | 1d |
| 6 | **告警通知** | API 500 > 阈值 → 企微/邮件通知 | 1d | — |

### 🟡 P1 — 核心运营能力（2-4 周）

| # | 功能 | 说明 |
|---|------|------|
| 7 | **用户详情页** | 查看单用户：stats、memories、personas、API keys、操作日志 |
| 8 | **用户封禁/解封** | 前端 + API |
| 9 | **DAU/MAU/留存 报表** | 自动化日/周/月报 |
| 10 | **试用管理** | 到期自动降级 + 手动延长试用 |
| 11 | **系统配置管理** | 环境变量 → DB 配置表 + Admin UI |
| 12 | **用量排行** | Top users by API calls / memories / personas |

### 🟢 P2 — 完善体验（后续迭代）

| # | 功能 |
|---|------|
| 13 | 邮件通知系统（注册欢迎、试用到期提醒、异常告警） |
| 14 | 用户行为漏斗分析（注册→激活→首次 Memory→活跃） |
| 15 | 自定义 Dashboard 图表（Grafana 集成或内置） |
| 16 | Stripe 支付 + 自动发票 |

---

## 四、数据统计方案（轻量级，不依赖外部 SaaS）

### 方案：PostgreSQL 物化视图 + 定时刷新

```
┌──────────────┐    每小时     ┌──────────────────┐
│  原始表       │ ──────────→  │  daily_stats       │
│  users        │   cron job   │  (日聚合快照)       │
│  memories     │              │                    │
│  audit_logs   │              │  - stat_date       │
│  sessions     │              │  - total_users     │
│  did_registry │              │  - new_registrations│
│  api_keys     │              │  - dau             │
└──────────────┘              │  - wau             │
                               │  - mau             │
                               │  - trial_activations│
                               │  - api_calls       │
                               │  - memory_creates  │
                               └──────────────────┘
```

### 实现步骤：

**Step 1：建表 `daily_stats`**
```sql
CREATE TABLE daily_stats (
    stat_date       DATE PRIMARY KEY,
    total_users     INTEGER DEFAULT 0,
    new_users       INTEGER DEFAULT 0,
    dau             INTEGER DEFAULT 0,
    wau             INTEGER DEFAULT 0,
    mau             INTEGER DEFAULT 0,
    trial_activated INTEGER DEFAULT 0,
    api_calls       INTEGER DEFAULT 0,
    memory_creates  INTEGER DEFAULT 0,
    persona_creates INTEGER DEFAULT 0,
    active_sessions INTEGER DEFAULT 0,
    updated_at      TIMESTAMPTZ DEFAULT now()
);
```

**Step 2：后端新增 `/api/admin/stats` 查询端点**（FastAPI）

**Step 3：Railway Cron Job**（或 Railway 内置 cron + Python 脚本每小时执行）

**Step 4：前端 Admin 看板**
- ECharts / Recharts 折线图渲染 DAU/注册趋势
- 无需外部 SaaS，纯 SQL + 前端图表

### DAU 计算逻辑：
```sql
-- DAU = 今天调用过 API 或创建过 session 的独立 user_id
SELECT COUNT(DISTINCT user_id) 
FROM audit_logs 
WHERE created_at::date = CURRENT_DATE;
```

---

## 五、告警方案

### 轻量级方案：Healthcheck 脚本 + 企微 Webhook

```
Railway Healthcheck (/health)
        │
        ▼
┌─────────────────┐     error_count > 阈值?     ┌──────────────┐
│ alert_checker.py │ ──────────────────────────→ │ 企微机器人     │
│ (cron: 每5分钟)   │                              │ (Webhook)    │
└─────────────────┘                              └──────────────┘
        │
        │ db 连通?
        ▼
┌─────────────────┐
│ 邮件通知 (可选)   │
└─────────────────┘
```

### 实现：
1. `scripts/alert_checker.py`：GET `/health`，检查 `error_count` 和 `db` 状态
2. 阈值规则：
   - `error_count` 30分钟内 > 10 → P1 告警
   - `db: false` → P0 告警
   - 连续 3 次 health check 失败 → P0 告警
3. 通知通道：企微机器人 Webhook（已有 wecom MCP 工具可复用）
4. 防抖：同一告警 30 分钟内不重复发送

---

## 六、数据库备份方案

### 当前风险评估：
- Supabase Free plan：**无自动备份**（Pro plan 才有）
- 数据集中在一处，无异地容灾

### 方案（按优先级）：

**方案 A — Supabase Pro（推荐，最小改动）**
- 升级到 Pro plan（$25/月）
- 自动每日备份 + 14 天保留
- 一键恢复

**方案 B — pg_dump cron（免费替代）**
```bash
# scripts/backup.sh — Railway cron job 每日执行
PGPASSWORD=$SUPABASE_DB_PASS pg_dump \
  -h $SUPABASE_DB_HOST \
  -U postgres \
  -d postgres \
  -Fc \
  -f /tmp/moltable_$(date +%Y%m%d).dump

# 上传到 S3 / R2 / GitHub Releases
```

**方案 C — Supabase CLI**
```bash
supabase db dump --db-url $DATABASE_URL -f backup_$(date +%Y%m%d).sql
```

### 备份策略：
| 层级 | 频率 | 保留期 | 方式 |
|------|------|--------|------|
| 实时 | 持续 | — | Supabase WAL（Pro plan） |
| 每日 | 每天 03:00 UTC | 30 天 | pg_dump → S3/R2 |
| 每周 | 每周日 | 12 周 | pg_dump → 本地下载 |
| 手动 | 发版前 | 永久 | `supabase db dump` |

**还缺：**
- 恢复演练脚本（每季度一次）
- 备份完整性校验（备份后立即验证可恢复）

---

## 七、速赢清单（本周可完成）

| 项目 | 工作量 | 效果 |
|------|:---:|------|
| `users` 表加 `last_active_at` 字段 | 0.5h | 可计算 DAU |
| `/health` 加 `user_count`、`db_latency_ms` | 0.5h | 基础监控 |
| Python logging 切 JSON 格式 | 0.5h | 日志可解析 |
| 企微 Webhook 告警脚本 | 2h | API 500 通知 |
| `daily_stats` 表 + 初始聚合 | 2h | DAU/注册量统计 |
| `GET /api/admin/stats` 基础端点 | 1h | Admin API 起步 |
| Admin 页面脚手架 | 2h | Admin 入口 |

**总计：~1 天可实现最小可用运营能力。**

---

## 八、总结

| 指标 | 当前 |
|------|:---:|
| 运营就绪度 | **16/100** |
| 产品功能完整度 | 85% ✅ |
| 技术架构 | A（90/100）✅ |
| 运营能力 | F（16/100）🔴 |

**结论：产品技术底子好，但运营基础设施几乎为零。现在用户少（< 100），投入 1-2 周就能把运营就绪度拉到 60+，足以支撑产品增长到 1000+ 用户。建议立即启动 P0 建设。**
